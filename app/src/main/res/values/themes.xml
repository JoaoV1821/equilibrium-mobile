<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Equilibrium" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Equilibrium" parent="Base.Theme.Equilibrium" />

    <!-- Provide a BottomNavigationView widget style placeholder (keeps compatibility with older Material versions). -->
    <style name="Widget.Equilibrium.BottomNav.NoActiveIndicator" parent="Widget.MaterialComponents.BottomNavigationView" />

    <style name="Theme.Equilibrium.NoNavIndicator" parent="Theme.Equilibrium">
        <!-- Apply the BottomNavigationView style via the theme attribute supported by MaterialComponents -->
        <item name="bottomNavigationStyle">@style/Widget.Equilibrium.BottomNav.NoActiveIndicator</item>
    </style>
</resources>